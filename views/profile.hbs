<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen py-12 px-4">
    <div class="max-w-2xl mx-auto bg-white rounded-lg shadow-lg p-8 border-l-4 border-emerald-500">
        <div class="flex items-center gap-3 mb-4">
            <span class="text-4xl">✅</span>
            <h1 class="text-3xl font-bold text-emerald-600">Welcome, {{fullName}}!</h1>
        </div>

        <p class="text-gray-600 mb-6">Registration successful.</p>

        <div class="grid md:grid-cols-2 gap-6">
            <div class="bg-gray-50 p-6 rounded-lg">
                <h2 class="text-lg font-semibold text-gray-800 mb-4">Your Details:</h2>
                <div class="space-y-3 text-gray-700">
                    <div>
                        <p class="text-sm text-gray-500">Email</p>
                        <p class="font-semibold">{{email}}</p>
                    </div>
                    <div>
                        <p class="text-sm text-gray-500">Course Track</p>
                        <p class="font-semibold">{{courseTrack}}</p>
                    </div>
                </div>

                <a href="/register" class="inline-block mt-6 text-blue-600 hover:underline">
                    ← Back to Register
                </a>
            </div>

            <div class="bg-gray-50 p-6 rounded-lg">
                <h2 class="text-lg font-semibold text-gray-800 mb-4">Profile Picture:</h2>
                <div class="bg-white p-3 rounded border flex justify-center">
                    <img src="{{imagePath}}" alt="Profile Picture" class="max-h-80 rounded-lg shadow" />
                </div>
                <p class="text-xs text-gray-500 mt-2 font-mono break-words">{{imagePath}}</p>
            </div>
        </div>
    </div>
</body>

</html>